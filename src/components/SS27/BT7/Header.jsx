import React from 'react';

const Header = () => {
    return (
        <header>
            <h1 style={{backgroundColor:'red', textAlign:'center'}}>Header</h1>
        </header>
    );
}

export default Header;
